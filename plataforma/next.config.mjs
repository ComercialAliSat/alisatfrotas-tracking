// Para deploy no Cloudflare Pages:
//   1. npm install  (instala @cloudflare/next-on-pages)
//   2. npm run pages:build  → gera .vercel/output/static
//   3. Configure build command no CF dashboard: npm run pages:build
//   4. Ative a flag de compatibilidade "nodejs_compat" no CF dashboard
// @see https://github.com/cloudflare/next-on-pages

/** @type {import('next').NextConfig} */
const nextConfig = {}

export default nextConfig
